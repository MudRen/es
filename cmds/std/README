 
  This bin directory contains all the standard user commands. All non-ghost
  users (including wizards) have access to the commands in this bin. Some
  commands react differently depending on whether the caller is a player
  or a wizard (ie: players get a -> prepended to their emotes).
 
