
    使用 who 指令时, 会列出每个玩家的名字和头衔(title). 对玩家而言, 这些
title 包含了所属职业及等级. 巫师则可随意使用字串更换其 title, 用法是:

set TITLE <字串>

如果字串中含有 $N, 那显示的结果将会包含巫师的名字. 举例来说, 

set TITLE $N is adding the wizhelp directory.

之後用 "review" 指令就能检视的以完成的 set 了. 