
to_int(3)            LPC LIBRARY FUNCTIONS              to_int(3)

NAME
     to_int - convert a float to an int

SYNOPSIS
     int to_int(float f);

DESCRIPTION
     The to_int() call returns the number of type `int'  that  is
     equivalent to the float `f' (with any decimal stripped off).

SEE ALSO
     to_float(3)


TMI-2 Release 1.1         Last change:                   93-09-12

