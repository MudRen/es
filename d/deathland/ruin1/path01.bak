
// Deathland/ruin1/path01.c

#include "../echobomber.h"

inherit ROOM;

void create()
{
	 ::create();
     set_short("dark path","黑黑的小径");
     set_long(@Long
Long
,@CLong
在这个隐密的大洞穴中 ,你查觉到这里与迷宫中其他地方有一些不同 ,这里的
空间比较开阔 .而且当你小心的把这个地方仔细地看了一遍後 ,你注意到有一条很
隐密的小径通向东方 .
CLong
);
     set("exits",([
         "west":Deathland"/dwarf/darkroom",
         "east":Deathland"/ruin1/path02",
         ]) );
     ::reset();
#include <../replace_room.h>
}

