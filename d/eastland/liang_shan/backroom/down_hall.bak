#include <takeda.h>

inherit ROOM;

void create()
{
	::create();
	set_short( "忠义厅楼下" );
	set_long(
	"这是一间小小的房间，四壁都是坚硬的石壁，东北方似乎有个小门。\n"
	);
        set( "exits", ([
                "up" : TROOM"hall",
                "northeast" : TROOM"jail2"
        
        ]) );
        reset();
}
